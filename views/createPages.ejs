<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <title>Create Course - LMS</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-900 text-white font-sans flex items-center min-h-screen">
    <!-- Header -->
    <!-- <%- include('header.ejs') %> -->
    <!-- Main -->
    <main class="w-full mx-auto max-w-md">
      <h2 class="text-xl font-semibold text-purple-300 mb-6">Add a New Page</h2>

      <form
        action="/course/<%= courseId %>/chapter/<%=chapterId %>/page/create"
        method="POST"
        class="bg-gray-800 p-6 rounded-lg shadow space-y-4"
      >
        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
        <!-- Title -->
        <div>
          <label for="title" class="block text-sm mb-1">Page Title</label>
          <input
            type="text"
            name="title"
            id="title"
            class="w-full px-4 py-2 rounded bg-gray-700 text-white border border-gray-600"
            required
          />
        </div>

        <!-- Content -->
        <div>
          <label for="content" class="block text-sm mb-1">Page Content</label>
          <textarea
            name="content"
            id="content"
            rows="4"
            class="w-full px-4 py-2 rounded bg-gray-700 text-white border border-gray-600"
            required
          ></textarea>
        </div>

        <!-- Submit -->
        <button
          type="submit"
          class="w-full bg-purple-700 hover:bg-purple-800 px-4 py-2 rounded-md text-white font-semibold"
        >
          Create Page
        </button>
      </form>
    </main>
  </body>
</html>
