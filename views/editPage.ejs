<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Edit Page | LMS</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Inter", sans-serif;
      }
    </style>
  </head>
  <body class="bg-gray-950 flex justify-center min-h-screen px-4">
    <div class="bg-gray-900 shadow-2xl p-6 sm:p-8 w-full max-w-md flex flex-col justify-center">
      <h1 class="text-xl sm:text-2xl font-bold text-purple-500 mb-6 text-center">Edit Page</h1>

      <% if (messages.success && messages.success.length > 0) { %>
        <p class="text-green-400 text-sm mb-2 text-center"><%= messages.success[0] %></p>
      <% } %>
      <% if (messages.error && messages.error.length > 0) { %>
        <p class="text-red-400 text-sm mb-2 text-center"><%= messages.error[0] %></p>
      <% } %>

      <form
        action="/page/<%= page.id %>?_method=PUT"
        method="POST"
        class="space-y-4"
      >
        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />

        <!-- Page Title -->
        <div>
          <label for="title" class="block mb-1 text-gray-300">Page Title</label>
          <input
            type="text"
            id="title"
            name="title"
            value="<%= page.title %>"
            class="w-full px-4 py-2 rounded-xl bg-gray-800 border border-gray-600 text-white focus:outline-none focus:ring-2 focus:ring-purple-500"
          />
        </div>

        <!-- Page Content -->
        <div>
          <label for="content" class="block mb-1 text-gray-300">Content</label>
          <textarea
            id="content"
            name="content"
            rows="8"
            class="w-full px-4 py-2 rounded-xl bg-gray-800 border border-gray-600 text-white resize-none focus:outline-none focus:ring-2 focus:ring-purple-500"
          ><%= page.content %></textarea>
        </div>

        <!-- Page Order -->
        <div>
          <label for="order" class="block mb-1 text-gray-300">Page Order</label>
          <input
            type="number"
            id="order"
            name="order"
            value="<%= page.order || 0 %>"
            min="1"
            class="w-full px-4 py-2 rounded-xl bg-gray-800 border border-gray-600 text-white focus:outline-none focus:ring-2 focus:ring-purple-500"
          />
        </div>

        <button
          type="submit"
          class="w-full bg-purple-600 hover:bg-purple-700 text-white font-semibold py-2 rounded-xl transition duration-200 mt-4"
        >
          Update Page
        </button>
      </form>
    </div>
  </body>
</html>
