<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>View Page | LMS</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-900 min-h-screen p-6">
    <header class="bg-gray-800 p-4 rounded-xl mb-6 shadow-md flex justify-between items-center">
      <h1 class="text-xl font-bold text-purple-500"><%= page.title %></h1>

      <!-- Exit Icon -->
      <a href="/course/<%= courseId %>/chapter/<%= page.chapterId %>" title="Exit">
        <div class="bg-gray-700 rounded-md hover:bg-gray-900">
        <svg xmlns="http://www.w3.org/2000/svg"
             fill="none" viewBox="0 0 24 24"
             stroke-width="1.5" stroke="currentColor"
             class="w-6 h-6 text-red-500 hover:text-red-700 transition">
          <path stroke-linecap="round" stroke-linejoin="round"
                d="M6 18 18 6M6 6l12 12" />
        </svg>
      </div>
      </a>
    </header>
    <div class="max-w-4xl mx-auto bg-gray-800 p-6 rounded-2xl shadow-md">
      <div class="">
        <% if (page.content === null) { %>
            <p class="text-gray-500">empty page.</p>
          <% } else { %>
            <p class="text-gray-300 "><%= page.content %></p>
          <% } %>
         </div>

    </div>
  </body>
</html>
