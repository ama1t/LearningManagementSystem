<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Manage Chapters</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-900 min-h-screen p-6">
    <header>
      <div class="bg-gray-800 flex justify-between mb-6 rounded-xl font-bold p-4">
        <h1 class="text-2xl font-bold text-purple-500"><%= chapter.title %></h1>
      </div>
    </header>
    <div class="max-w-4xl mx-auto bg-gray-800 p-6 rounded-2xl shadow-md">
      <div class="flex justify-between items-center mb-4">
        <h1 class="text-2xl font-semibold text-purple-500">Pages</h1>
      </div>
      <div class="mb-8">
        <% if (pages.length === 0) { %>
            <p class="text-gray-500">No pages added yet.</p>
          <% } else { %>
            <ul class="space-y-4">
              <% pages.forEach((page, index) => { %>
                <li class="bg-gray-700 p-4 rounded-xl shadow flex justify-between items-center">
                  <div>
                    <h2 class="text-lg font-semibold text-white">
                      <span class="text-white font-bold mr-2">L<%= index + 1 %>:</span>
                      <a href="/chapter/<%= chapter.id %>/pages" class="hover:underline hover:text-indigo-600">
                        <%= page.title %>
                      </a>
                    </h2>
                    <p class="text-sm text-gray-300"><%= page.description %></p>
                  </div>
                  <div class="flex gap-2">
                    <a href="/chapter/edit/<%= chapter.id %>" class="bg-yellow-500 hover:bg-yellow-600 text-white px-3 py-1 rounded-md text-sm">Edit</a>
                    <form action="/chapter/delete/<%= chapter.id %>" method="POST">
                      <button type="submit" class="bg-red-500 hover:bg-red-600 text-white px-3 py-1 rounded-md text-sm">Delete</button>
                    </form>
                  </div>
                </li>
              <% }); %>
            </ul>
          <% } %>
          
      </div>
      <a
        href="/course/<%= courseId %>/chapter/create"
        class="bg-indigo-600 hover:bg-indigo-700 text-white px-4 py-2 rounded-xl shadow"
      >
        Add New Chapter
      </a>

    </div>
  </body>
</html>
