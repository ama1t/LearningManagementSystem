<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Create Chapter | LMS</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Inter", sans-serif;
      }
    </style>
  </head>

  <body class="bg-gray-950 flex justify-center min-h-screen px-4">
    <div
      class="bg-gray-900 shadow-2xl p-6 sm:p-8 w-full max-w-md flex flex-col items-center justify-center"
    >
      <h2
        class="text-xl sm:text-2xl font-bold text-center mb-6 text-purple-500"
      >
        Create New Chapter
      </h2>

      <form
        action="/course/<%= courseId %>/chapter/create"
        method="POST"
        class="space-y-4 w-full"
      >
        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />

        <input
          type="text"
          name="title"
          placeholder="Chapter Title"
          required
          class="w-full px-4 py-2 border border-gray-600 rounded-xl bg-gray-900 text-white focus:outline-none focus:ring-2 focus:ring-purple-500"
        />

        <textarea
          name="description"
          placeholder="Chapter Description"
          rows="5"
          required
          class="w-full px-4 py-2 border border-gray-600 rounded-xl bg-gray-900 text-white resize-none focus:outline-none focus:ring-2 focus:ring-purple-500"
        ></textarea>

        <button
          type="submit"
          class="w-full bg-purple-600 hover:bg-purple-700 text-white font-semibold py-2 rounded-xl transition duration-200"
        >
          Create Chapter
        </button>
      </form>
    </div>
  </body>
</html>
