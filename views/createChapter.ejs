<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <title>Create Chapter | LMS</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-900 text-white font-sans flex items-center min-h-screen">
    <main class="w-full mx-auto max-w-md">
      <form
        action="/course/<%= courseId %>/chapter/create"
        method="POST"
        class="bg-gray-800 p-6 rounded-lg shadow space-y-4"
      >
        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
        <!-- Title -->
        <div>
          <h2 class="text-2xl font-bold text-purple-500 mb-6 text-center">
            Create a New Chapter
          </h2>
          <label for="title" class="block text-sm mb-1">Chapter Title</label>
          <input
            type="text"
            name="title"
            id="title"
            class="w-full px-4 py-2 rounded bg-gray-700 text-white border border-gray-600"
            required
          />
        </div>

        <!-- Description -->
        <div>
          <label for="description" class="block text-sm mb-1"
            >Chapter Description</label
          >
          <textarea
            name="description"
            id="description"
            rows="4"
            class="w-full px-4 py-2 rounded bg-gray-700 text-white border border-gray-600"
            required
          ></textarea>
        </div>

        <!-- Submit -->
        <button
          type="submit"
          class="w-full bg-purple-700 hover:bg-purple-800 px-4 py-2 rounded-md text-white font-semibold"
        >
          <a href="/chapter/create"></a> Create Chapter
        </button>
      </form>
    </main>
  </body>
</html>
