<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <title>Create Course - LMS</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-900 text-white font-sans min-h-screen">
    <!-- Header -->
    <%- include('header.ejs') %>
    <!-- Main -->
    <main class="p-8 max-w-2xl mx-auto">
      <h2 class="text-xl font-semibold text-purple-300 mb-6">
        Create a New Course
      </h2>

      <form
        action="/addcourse"
        method="POST"
        class="bg-gray-800 p-6 rounded-lg shadow space-y-4"
      >
        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
        <!-- Title -->
        <div>
          <label for="title" class="block text-sm mb-1">Course Title</label>
          <input
            type="text"
            name="title"
            id="title"
            class="w-full px-4 py-2 rounded bg-gray-700 text-white border border-gray-600"
            required
          />
        </div>

        <!-- Description -->
        <div>
          <label for="description" class="block text-sm mb-1"
            >Course Description</label
          >
          <textarea
            name="description"
            id="description"
            rows="4"
            class="w-full px-4 py-2 rounded bg-gray-700 text-white border border-gray-600"
            required
          ></textarea>
        </div>

        <!-- Image URL -->
        <div>
          <label for="imageUrl" class="block text-sm mb-1"
            >Course Image URL</label
          >
          <input
            type="url"
            name="imageUrl"
            id="imageUrl"
            class="w-full px-4 py-2 rounded bg-gray-700 text-white border border-gray-600"
          />
        </div>

        <!-- Submit -->
        <button
          type="submit"
          class="w-full bg-purple-700 hover:bg-purple-800 px-4 py-2 rounded-md text-white font-semibold"
        >
          Create Course
        </button>
      </form>
    </main>
  </body>
</html>
