<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Edit Chapter | LMS</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Inter", sans-serif;
      }
    </style>
  </head>

  <body class="bg-gray-950 flex justify-center min-h-screen px-4">
    <div class="bg-gray-900 shadow-2xl p-6 sm:p-8 w-full max-w-md flex flex-col justify-center">
      <h1 class="text-xl sm:text-2xl font-bold text-purple-500 mb-6 text-center">Edit Chapter</h1>

      <% if (messages.error && messages.error.length > 0) { %>
        <p class="text-red-400 text-sm mb-4 text-center"><%= messages.error[0] %></p>
      <% } %>

      <form
        action="/chapter/<%= chapter.id %>?_method=PUT"
        method="POST"
        class="space-y-4"
      >
        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />

        <!-- Chapter Title -->
        <div>
          <label for="title" class="block mb-1 text-gray-300">Chapter Title</label>
          <input
            type="text"
            id="title"
            name="title"
            value="<%= chapter.title %>"
            class="w-full px-4 py-2 border border-gray-700 rounded-xl bg-gray-800 text-white focus:outline-none focus:ring-2 focus:ring-purple-500"
          />
        </div>

        <!-- Description -->
        <div>
          <label for="description" class="block mb-1 text-gray-300">Description</label>
          <textarea
            id="description"
            name="description"
            rows="6"
            class="w-full px-4 py-2 border border-gray-700 rounded-xl bg-gray-800 text-white resize-none focus:outline-none focus:ring-2 focus:ring-purple-500"
          ><%= chapter.description %></textarea>
        </div>

        <!-- Order -->
        <div>
          <label for="order" class="block mb-1 text-gray-300">Display Order</label>
          <input
            type="number"
            id="order"
            name="order"
            value="<%= chapter.order %>"
            min="1"
            class="w-full px-4 py-2 border border-gray-700 rounded-xl bg-gray-800 text-white focus:outline-none focus:ring-2 focus:ring-purple-500"
          />
        </div>

        <!-- Submit Button -->
        <button
          type="submit"
          class="w-full bg-purple-600 hover:bg-purple-700 text-white font-semibold py-2 rounded-xl transition duration-200 mt-4"
        >
          Update Chapter
        </button>
      </form>
    </div>
  </body>
</html>
