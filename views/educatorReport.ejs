<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Course Report | LMS</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-950 text-white">

  <!-- Header -->
  <%- include('header.ejs') %>

  <main class="max-w-5xl mx-auto px-4 sm:px-6 pt-24">
    <h1 class="text-xl font-semibold mb-6 flex items-center gap-2">
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"
           stroke-width="1.5" stroke="currentColor" class="h-7 w-7 text-green-400">
        <path stroke-linecap="round" stroke-linejoin="round"
              d="M7.5 14.25v2.25m3-4.5v4.5m3-6.75v6.75m3-9v9M6 
              20.25h12A2.25 2.25 0 0 0 20.25 18V6A2.25 2.25 0 
              0 0 18 3.75H6A2.25 2.25 0 0 0 3.75 6v12A2.25 
              2.25 0 0 0 6 20.25Z" />
      </svg>
      Course Reports
    </h1>

    <% if (reportData.length === 0) { %>
      <p class="text-gray-400">No courses created yet.</p>
    <% } else { %>
      <div class="overflow-x-auto rounded-lg border border-gray-700">
        <table class="min-w-full text-left text-sm">
          <thead class="bg-gray-800 text-gray-300 uppercase text-xs">
            <tr>
              <th class="px-4 py-3">#</th>
              <th class="px-4 py-3">Course Title</th>
              <th class="px-4 py-3">Enrolled</th>
              <th class="px-4 py-3">Completed</th>
              <th class="px-4 py-3">Completion %</th>
            </tr>
          </thead>
          <tbody>
            <% reportData.forEach((course, index) => { %>
              <tr class="border-t border-gray-700 hover:bg-gray-800">
                <td class="px-4 py-2"><%= index + 1 %></td>
                <td class="px-4 py-2 font-medium text-white"><%= course.title %></td>
                <td class="px-4 py-2 text-blue-400 font-semibold"><%= course.totalStudents %></td>
                <td class="px-4 py-2 text-green-400 font-semibold"><%= course.completedStudents %></td>
                <td class="px-4 py-2 text-yellow-400 font-semibold"><%= course.completionPercentage %> %</td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    <% } %>
  </main>

</body>
</html>
